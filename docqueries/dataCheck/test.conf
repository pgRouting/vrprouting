#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'tests' => [qw(
            data_check
            create_initial_solution
            )],

        'documentation' => [qw(
            data_check
            create_initial_solution
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
