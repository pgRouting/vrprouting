#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'data' => ['functions.sql'],
        'tests' => [qw(
            sample_output
            sample-with-service
            )],

        'documentation' => [qw(
            sample_output
            sample-with-service
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
