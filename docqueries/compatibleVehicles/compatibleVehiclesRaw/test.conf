#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'data' => ['functions.sql'],
        'newdb' => 1,
        'tests' => [qw(
            new_db
            order-20
            sample_output
            )],

        'documentation' => [qw(
            new_db
            order-20
            sample_output
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
