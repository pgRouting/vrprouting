#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'data' => [],
        'tests' => [qw(
            _vehicles_at_time
            )],

        'documentation' => [qw(
            _vehicles_at_time
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
