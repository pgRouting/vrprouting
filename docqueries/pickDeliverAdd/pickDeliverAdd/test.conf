#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'tests' => [qw(
            pickDeliverAdd
            )],

        'documentation' => [qw(
            pickDeliverAdd
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
