#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'data' => ['functions.sql'],
        'tests' => [qw(
            pickDeliverAdd
            )],

        'documentation' => [qw(
            pickDeliverAdd
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
