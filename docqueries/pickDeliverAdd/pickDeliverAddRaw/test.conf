#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'tests',
        'tests' => [qw(
            pickDeliverAddRaw
            )],

        'documentation' => [qw(
            pickDeliverAddRaw
            )],

        'nottesting' => [qw(
            )]
    },
);

1;
